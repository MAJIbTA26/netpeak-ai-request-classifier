# Request Classifier — тестове завдання Netpeak AI Solutions

Python-сервіс, який читає вхідний "інбокс" запитів у вільній формі,
класифікує кожен запит через LLM (Gemini) у строгу структуру, і формує
короткий агрегований звіт.

## Як запустити

### 1. Встанови залежності

```bash
pip install -r requirements.txt
```

### 2. Отримай безкоштовний ключ Gemini API

1. Зайди на [aistudio.google.com/apikey](https://aistudio.google.com/apikey)
2. Увійди через Google-акаунт
3. Створи ключ (безкоштовно, без картки)

### 3. Задай ключ через .env

```bash
cp .env.example .env
```

Відкрий `.env` і встав свій реальний ключ:
```
GEMINI_API_KEY=твій_реальний_ключ
```

### 4. Запусти

```bash
python main.py
```

Скрипт прочитає `input_requests.csv`, обробить кожен запит і створить:
- `output.json` — повний структурований результат по всіх запитах
- `report.md` — короткий звіт з агрегатами

## Структура схеми виводу

Обов'язкові поля з завдання: `id`, `category`, `target_department`,
`priority`, `short_summary`, `requested_actions`, `needs_clarification`.

**Розширення схеми:** додано поле `processing_status` (`"ok"` / `"failed"`).

**Чому:** завдання explicit вимагає "коректно обробляти випадки, коли
модель повернула невалідні дані". Просто мовчки пропустити такий запит
або впасти з винятком — погані варіанти для реального продакшн-сервісу.
Замість цього кожен запит, який не вдалось коректно обробити навіть
після повторних спроб, все одно потрапляє в `output.json` і `report.md`,
але з чітким маркером `processing_status: "failed"` — так команда бачить
**усі** вхідні запити, включно з проблемними, і може розібрати їх вручну,
замість того щоб вони "губились" без сліду.

**Технічна деталь схеми:** поле `target_department` — це `str = ""`
(порожній рядок замість `None`/`null`), а не `Optional[str]`. Причина
описана нижче в розділі "Де рішення ламається" — це не довільний вибір
стилю, а обхід реальної несумісності між Pydantic `Optional` та форматом
структурованого виводу Gemini API.

## Де рішення ламається / обмеження

### Невалідний вивід LLM
Обробляється через retry-логіку (`classifier.py`): до 3 повторних спроб
з **exponential backoff** (3с → 6с → 12с → 24с) при `JSONDecodeError`,
`ValidationError` чи помилках API. Якщо всі спроби невдалі — запит
потрапляє в результат зі статусом `processing_status: "failed"`
(див. вище), а не ламає обробку решти файлу.

**Реальний кейс, з яким зіткнувся під час розробки:** Pydantic-поле
`target_department: Optional[str] = None` викликало помилку валідації
на боці `google-genai` SDK при конвертації схеми в Gemini structured
output (`Input should be 'TYPE_UNSPECIFIED', 'STRING', ... [type=literal_error, input_value='NULL']`)
— внутрішній формат Schema, який Gemini API використовує для structured
output, на момент розробки не приймав `null` як тип поля так, як це
робить Pydantic/JSON Schema. Рішення: замінив `Optional[str] = None` на
`str = ""` (порожній рядок як sentinel-значення "не визначено"), і
відповідно скоригував промт та обробку в `report.md`.

**Обмеження:** зараз retry для помилок валідації використовує той самий
промт без змін. У продакшн-версії варто додати до повторного промта
текст помилки, щоб LLM могла сама виправити конкретну проблему.

### Нестабільність версій моделей провайдера
Спочатку код мав захардкоджену резервну модель `gemini-2.5-flash`. Під
час реального запуску (не в тестах, а на живих даних) Google API
повернув `404 NOT_FOUND`: ця модель **вже недоступна для нових
користувачів**, з прямою рекомендацією перейти на `gemini-3.6-flash`
у тілі самої помилки. Це показує, наскільки швидко змінюється модельний
ряд LLM-провайдерів навіть у межах кількох місяців — той самий клас
проблем, з яким я вже стикався раніше при роботі з Groq API (там
`llama-3.3-70b-versatile` теж виявилась deprecated).

**Що б зробив у продакшн-версії:** замість захардкодженої назви моделі
— перевірка доступних моделей через `client.models.list()` при старті
сервісу (є такий скрипт-хелпер з мого іншого pet-проєкту), з falls-back
на список кандидатів за пріоритетом, а не на єдину жорстко задану назву.

Окремо стикнувся з тимчасовими `503 UNAVAILABLE` (перевантаження
безкоштовного тіру моделі `gemini-flash-latest` в моменти пікового
навантаження) — це вирішується саме exponential backoff та fallback на
альтернативну модель, які вже реалізовані.

### Великий обсяг запитів
Безкоштовний тір Gemini має ліміт **~10 запитів на хвилину**. Для 18
тестових запитів це не проблема (є штучна затримка `1.5` сек між
запитами в `main.py`), але для сотень/тисяч запитів на день такий
послідовний підхід буде занадто повільним і почне впиратись у rate
limit ще сильніше, ніж я вже спостерігав на малому обсязі.

**Що зробив би далі:** перехід на **Batch API** Gemini (асинхронна
пакетна обробка, дешевша на ~50%, без rate-limit проблем для великих
обсягів) або на платний тір з вищими лімітами + `asyncio` для
паралельних запитів.

### Недетермінізм LLM
Виставлено `temperature=0` у конфігурації запиту — це **зменшує**, але
**не гарантує повну відсутність** варіативності відповідей LLM між
запусками (природа мовних моделей). Для критичних до консистентності
задач варто додатково: (1) кешувати результати по хешу тексту запиту,
щоб той самий текст завжди давав той самий результат без повторного
виклику LLM, (2) розглянути few-shot приклади в промті для стабілізації
формату відповідей.

### Вартість токенів
Для 18 запитів у цьому завданні вартість токенів **практично нульова**
(вписується в безкоштовний тір). Для продакшн-обсягів (тисячі запитів
на день) варто: (1) використовувати `gemini-flash-lite` замість
`gemini-flash` для простих класифікаційних задач (значно дешевше), (2)
кешувати повторювані запити, (3) моніторити вартість через Google Cloud
Billing alerts.

## Що зробив би далі, якби було більше часу

Із опціонального списку в завданні — пріоритетно додав би:

1. **Async/батчева обробка** — замість послідовного `for`-циклу з `sleep`,
   паралельна обробка через `asyncio.gather` (з обмеженням одночасних
   запитів через `Semaphore`, щоб не впертись у rate limit) — суттєво
   пришвидшить обробку великих файлів.

2. **Telegram-дайджест** — надсилання короткого звіту в Telegram-чат
   команди після завершення обробки (маю вже готовий, перевірений досвід
   роботи з Telegram Bot API з іншого pet-проєкту).

3. **Docker-образ** — для відтворюваного запуску без залежності від
   локального середовища Python.

4. Запис у Google Sheets залишив би останнім у пріоритеті — потребує
   OAuth-налаштування, що виходить за рамки 2-3 годин, відведених на
   тестове.

## Тестування

Проєкт покритий базовими автотестами (`pytest`) для найкритичніших
частин рішення — тих самих, які виявились слабким місцем за фідбеком
після першої версії тестового завдання:

- **`test_schema.py`** — перевіряє, що схема `RequestAnalysis` коректно
  приймає валідні дані та відхиляє категорію поза дозволеним списком.
- **`test_classifier.py`** — перевіряє логіку розпізнавання типів помилок
  API (`rate_limit`, `temporary_unavailable`, `model_not_found`) та
  парсингу рекомендованого часу очікування з тексту помилки Google.

### Запуск тестів

```bash
pip install pytest
pytest -v
```

Очікуваний результат: 7 passed.

## Структура проєкту

```
netpeak-test-task/
├── main.py              # orchestration: CSV -> LLM -> output.json + report.md
├── classifier.py         # виклик Gemini API, retry-логіка, обробка помилок
├── schema.py              # Pydantic-схема структурованого виводу
├── test_schema.py          # автотести для схеми
├── test_classifier.py       # автотести для обробки помилок
├── input_requests.csv        # вхідні дані (надані)
├── requirements.txt
├── .env.example
└── README.md
```
